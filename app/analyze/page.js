import AnalyzeHub from "../../components/AnalyzeHub";

export const metadata = {
  title: "Deal Analyzer \u2014 DealScout",
  description: "Analyze any small business acquisition in 60 seconds. Upload a CIM for instant extraction. SDE recast, SBA loan calculator, valuation scoring, stress testing, offer strategy, and deal pipeline tracking.",
};

export default function AnalyzePage() {
  return <AnalyzeHub />;
}
